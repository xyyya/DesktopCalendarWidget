<Window x:Class="DesktopCalendarWidget.NoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="便签" Width="340" Height="390"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseEnter="Window_MouseEnter"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="NoteTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushKey}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentLightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NoteComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Background" Value="{DynamicResource DropDownBg}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                                BorderThickness="0" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ControlHoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NoteComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ControlBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubBorderBrushKey}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MaxDropDownHeight" Value="220"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource NoteComboItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBorder" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="7">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="22"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center" HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Width="8" Height="5"
                                          Fill="{DynamicResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <ToggleButton Grid.ColumnSpan="2" Focusable="False" Background="Transparent" BorderThickness="0"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton"><Border Background="Transparent"/></ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade" StaysOpen="False" Panel.ZIndex="9999">
                                <Border Background="{DynamicResource DropDownBg}" BorderBrush="{DynamicResource SubBorderBrushKey}"
                                        BorderThickness="1" CornerRadius="8" Padding="4" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentLightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder" CornerRadius="16" BorderThickness="1.5" Padding="12"
            Background="{DynamicResource WindowBg}" BorderBrush="{DynamicResource BorderBrushKey}"
            PreviewMouseLeftButtonDown="RootBorder_PreviewMouseLeftButtonDown">
        <Grid>
            <Grid x:Name="EditorContent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10" PreviewMouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Cursor="SizeAll">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TitleBar" Grid.Column="0" Text="便签" FontSize="14" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="✕" Click="Close_Click" Width="26" Height="24"
                            Background="Transparent" Foreground="{DynamicResource TextSecondary}" BorderThickness="0"
                            Cursor="Hand" ToolTip="关闭" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="118"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="lblNoteName" Text="便签名" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="txtTitle" Grid.Column="1" FontSize="15" FontWeight="Bold"
                             Style="{StaticResource NoteTextBoxStyle}" Margin="0,0,8,0"/>
                    <ComboBox x:Name="cmbTask" Grid.Column="2" Style="{StaticResource NoteComboStyle}"
                              SelectionChanged="Task_SelectionChanged" ToolTip="关联任务"/>
                </Grid>

                <TextBox x:Name="txtContent" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto"
                         Style="{StaticResource NoteTextBoxStyle}" Padding="9"
                         CaretBrush="{DynamicResource TextPrimary}"/>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <TextBlock x:Name="lblFontSize" Text="字号" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="cmbFontSize" Width="62" Style="{StaticResource NoteComboStyle}" SelectionChanged="FontStyle_Changed">
                        <ComboBoxItem Content="11"/><ComboBoxItem Content="13"/><ComboBoxItem Content="15"/><ComboBoxItem Content="18"/><ComboBoxItem Content="22"/><ComboBoxItem Content="28"/>
                    </ComboBox>
                    <TextBlock x:Name="lblFontColor" Text="颜色" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox x:Name="cmbFontColor" Width="82" Style="{StaticResource NoteComboStyle}" SelectionChanged="FontStyle_Changed">
                        <ComboBoxItem Content="默认" Tag=""/>
                        <ComboBoxItem Content="白色" Tag="#FFF0F0F0"/>
                        <ComboBoxItem Content="蓝色" Tag="#FF60A5FA"/>
                        <ComboBoxItem Content="绿色" Tag="#FF34D399"/>
                        <ComboBoxItem Content="黄色" Tag="#FFFBBF24"/>
                        <ComboBoxItem Content="红色" Tag="#FFF87171"/>
                    </ComboBox>
                </StackPanel>

                <Grid Grid.Row="4" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="chkCompleted" Content="完成" Foreground="{DynamicResource TextPrimary}"
                              VerticalAlignment="Center" Checked="Completed_Changed" Unchecked="Completed_Changed"/>
                    <TextBlock x:Name="LinkedLabel" Grid.Column="1" Foreground="{DynamicResource TextMuted}"
                               FontSize="10" VerticalAlignment="Center" Margin="8,0,8,0"/>
                    <Button x:Name="btnSave" Grid.Column="2" Content="保存" Click="Save_Click" Width="58" Height="30"
                            Background="{DynamicResource AccentBrush}" Foreground="#FFFFFFFF" BorderThickness="0"
                            Cursor="Hand"/>
                    <Button x:Name="btnDelete" Grid.Column="3" Content="删除" Click="Delete_Click" Width="58" Height="30"
                            Background="{DynamicResource ControlBg}" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"
                            Margin="6,0,0,0" Cursor="Hand"/>
                </Grid>
            </Grid>

            <Border x:Name="CapsuleContent" Visibility="Collapsed" Width="64" Height="26"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    MouseLeftButtonDown="Capsule_MouseLeftButtonDown" Cursor="SizeAll"
                    Background="{DynamicResource ControlBg}" BorderBrush="{DynamicResource AccentLightBrush}"
                    BorderThickness="1" CornerRadius="13" Padding="5,2">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="✎" FontSize="12" Foreground="{DynamicResource AccentLightBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="便签" FontSize="10" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" Margin="3,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
